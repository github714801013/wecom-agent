services:
  wecom-agent:
    image: wecom-agent:latest
    container_name: wecom-agent
    restart: always
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
