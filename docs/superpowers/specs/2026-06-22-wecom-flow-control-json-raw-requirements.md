# 原始需求归档

## 原始文本需求

> [$dev-spec-gen](D:\workplace\skills\my-skills\dev-spec-gen\SKILL.md) 1. 优化流程节点之间都是通过json 来输出，比如下一个节点是否需要sql 语句检验，是否需要覆盖前面的流等等，由上游节点决定下游节点是否需要执行
>
> 用户确认：确认

## 来源

- 对话/链接：当前 Codex 对话
- Jira/Yuque/文档：无

## 附件语义化记录

| 附件 | 来源/文件名 | 可见内容语义化描述 | 待确认点 |
| :--- | :--- | :--- | :--- |
| 无 | 无 | 无 | 无 |

## 初步验收断言

- [ ] 支持上游节点通过 JSON 协议声明下游是否需要执行 SQL 审核。
- [ ] 支持上游节点通过 JSON 协议声明后续可见内容是否覆盖前面流式内容。
- [ ] 协议内容不会展示给企业微信用户。
- [ ] 未输出协议时保持原默认行为：继续执行 SQL 审核，流式内容按原逻辑追加或累计替换。
- [ ] 非法协议内容不影响主流程，回退到默认控制。
- [ ] 最小闭环只覆盖 `runSqlAudit` 和 `coverPrevious`，不做全链路节点协议重构。

## 不适用或暂缓项

- 不涉及数据库结构变更。
- 不涉及对外 API 契约变更。
- 不涉及 Java/Spring 测试入口。
